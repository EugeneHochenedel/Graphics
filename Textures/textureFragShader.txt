#version 410

in vec2 vTexCoord;
out vec4 FragColor;
uniform sampler2D diffuse;
uniform sampler2D combined;

void main()
{
	
	FragColor = texture(diffuse, vTexCoord).rgra * texture(combined, vTexCoord);
}